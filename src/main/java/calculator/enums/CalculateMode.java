package calculator.enums;

/**
 * <p>{@code CalculateMode} enumeration allows a {@code Calculator} class to calculate using either the degree or radian.</p>
 *
 * @author Seungmin Yang
 */
public enum CalculateMode {
    /**
     * <b>Degree</b><br>
     * Degree is a measurement of a plane angle in which one full rotation is 360 degrees, usually denoted by °.
     */
    DEG,

    /**
     * <b>Radian</b><br>
     */
    RAD,
}
